/**
 *
 * @param {array} array - An array
 * @return {bool} if the parameter is an arry with length
 *
 */

const isArrayWithLenth = array => array && Array.isArray(array) && array.length;

export default isArrayWithLenth;
